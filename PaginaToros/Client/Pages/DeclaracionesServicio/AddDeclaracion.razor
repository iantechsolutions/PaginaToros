@page "/declaraciones/{Id:int}"


@using Blazored.Typeahead
@using PaginaToros.Client.Servicios.Contrato;
@inject IDesepla1Servicio _desepla1Servicio
@inject IDesepla3Servicio _desepla3Servicio
@inject ISocioServicio _socioServicio
@inject IPlantelServicio _plantelServicio
@inject ITorosServicio _torosServicio
@inject ICertifsemanServicio _certifsemanServicio
@inject HttpClient http
@inject NavigationManager Navigation
@inject AuthenticationStateProvider AuthenticationStateProvider
@using System.Security.Claims


<style>
    .td {
        font-size: 11px !important;
    }

    .bm-title {
        font-size: 25px;
        font-weight: bold;
    }

    table span.rz-calendar input {
        color: yourDesiredColor;
    }

    .declaracion-loading-card {
        border: 1px solid #e8ecef;
        border-radius: 18px;
        background: #fff;
        box-shadow: 0 12px 32px rgba(15, 23, 42, 0.06);
        padding: 1.5rem;
    }

    .declaracion-skeleton {
        position: relative;
        overflow: hidden;
        background: #eef2f6;
        border-radius: 12px;
    }

    .declaracion-skeleton::after {
        content: "";
        position: absolute;
        inset: 0;
        transform: translateX(-100%);
        background: linear-gradient(90deg, transparent, rgba(255,255,255,0.75), transparent);
        animation: declaracion-shimmer 1.3s infinite;
    }

    .declaracion-skeleton-line {
        height: 14px;
        margin-bottom: .75rem;
    }

    .declaracion-skeleton-input {
        height: 42px;
        margin-bottom: 1rem;
    }

    .declaracion-skeleton-card {
        height: 88px;
        margin-bottom: 1rem;
    }

    .declaracion-skeleton-grid {
        height: 240px;
    }

    .declaracion-loading-text {
        color: #5b6470;
        font-size: .95rem;
        margin-bottom: 1rem;
    }

    .declaracion-grid {
        border: 1px solid #e6ebf0;
        border-radius: 12px;
        overflow: hidden;
        background: #fff;
    }

    .declaracion-grid .rz-datatable-data tr {
        transition: background-color 0.15s ease, box-shadow 0.15s ease;
        cursor: pointer;
    }

    .declaracion-grid .rz-datatable-data tr.declaracion-row-selected {
        background: #e8f5ff;
        box-shadow: inset 0 0 0 1px #7cc4ff;
    }

    .declaracion-grid .rz-datatable-data tr.declaracion-row-selected:hover {
        background: #dff1ff;
    }

    .declaracion-grid .rz-datatable-data tr.declaracion-row-disabled {
        background: #f2f4f7;
        color: #8a94a3;
        cursor: not-allowed;
        opacity: 0.7;
        pointer-events: none;
    }

    .declaracion-grid .rz-datatable-data tr:hover {
        background: #f6f9fc;
    }

    .declaracion-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.35rem;
        padding: 0.1rem 0.45rem;
        border-radius: 999px;
        font-size: 0.72rem;
        font-weight: 600;
        background: #e8f5ff;
        color: #0b4f8a;
        border: 1px solid #b6e1ff;
    }

    .declaracion-section {
        border: 1px solid #e6ebf0;
        border-radius: 14px;
        background: #fff;
        padding: 1rem 1.25rem 1.25rem 1.25rem;
        box-shadow: 0 8px 24px rgba(15, 23, 42, 0.05);
        margin-bottom: 1rem;
    }

    .declaracion-section-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 0.75rem;
    }

    .declaracion-section-title {
        font-weight: 700;
        font-size: 1.05rem;
        color: #1f2937;
    }

    .declaracion-selector {
        max-width: 360px;
        margin-left: auto;
    }

    @@keyframes declaracion-shimmer {
        100% {
            transform: translateX(100%);
        }
    }

    }
</style>
<button class="bm-title" @onclick="ComeBack"><FeatherArrowLeft Color="Black"></FeatherArrowLeft></button>
@if (isInitialLoading)
{
    <div class="declaracion-loading-card mt-3">
        <div class="declaracion-loading-text">@loadingMessage</div>
        <div class="row">
            <div class="col-sm">
                <div class="declaracion-skeleton declaracion-skeleton-line" style="width: 35%;"></div>
                <div class="declaracion-skeleton declaracion-skeleton-input"></div>
            </div>
            <div class="col-sm">
                <div class="declaracion-skeleton declaracion-skeleton-line" style="width: 35%; margin-left:auto;"></div>
                <div class="declaracion-skeleton declaracion-skeleton-input"></div>
            </div>
            <div class="col-sm">
                <div class="declaracion-skeleton declaracion-skeleton-line" style="width: 35%;"></div>
                <div class="declaracion-skeleton declaracion-skeleton-input"></div>
            </div>
        </div>
        <div class="row mt-2">
            <div class="col-sm">
                <div class="declaracion-skeleton declaracion-skeleton-card"></div>
            </div>
            <div class="col-sm">
                <div class="declaracion-skeleton declaracion-skeleton-card"></div>
            </div>
        </div>
        <div class="declaracion-skeleton declaracion-skeleton-grid mt-2"></div>
    </div>
}
else if (socios.Count > 0)
{
    <div class="card">
        <div class="card-body">
            <EditForm Model="@oDeclaracion" OnSubmit="HandleSubmit" autocomplete="off">
                <DataAnnotationsValidator />
                <div class="row" style="width=100%;">
                    <div class="col-sm">
                        @if (!esSocio )
                        {
                            <BlazoredTypeahead SearchMethod="BusquedaSocios"
                                               placeholder="Busque socio por nombre"
                                               EnableDropDown="true"
                                               TItem="SocioDTO"
                                               TValue="SocioDTO"
                                               Disabled="@(Id != 0)"
                                               Value="socioSeleccionado"
                                               ValueChanged="UpdateValores"
                                               ValueExpression="@(() => socioSeleccionado)">
                                <SelectedTemplate Context="eleccion">
                                    @eleccion.Nombre
                                </SelectedTemplate>
                                <ResultTemplate Context="eleccion">
                                    @eleccion.Nombre
                                </ResultTemplate>
                            </BlazoredTypeahead>
                        }
                        
                    </div>

                     <div class="col-sm text-end">
                 

                            @if (Id != 0)
                            {
                                <button type="button"
                                        class="btn btn-danger"
                                        @onclick="EliminarDeclaracion">
                                    Eliminar declaración
                                </button>
                            }
                        <button type="submit" class="btn btn-primary mr-2">Aceptar</button>
                        

                    </div> 
                </div>
                <br />
                @if (isLoadingDependencias)
                {
                    <div class="declaracion-loading-card">
                        <div class="declaracion-loading-text">@loadingMessage</div>
                        <div class="row">
                            <div class="col-sm">
                                <div class="declaracion-skeleton declaracion-skeleton-line" style="width: 30%;"></div>
                                <div class="declaracion-skeleton declaracion-skeleton-input"></div>
                            </div>
                            <div class="col-sm">
                                <div class="declaracion-skeleton declaracion-skeleton-line" style="width: 30%;"></div>
                                <div class="declaracion-skeleton declaracion-skeleton-input"></div>
                            </div>
                            <div class="col-sm">
                                <div class="declaracion-skeleton declaracion-skeleton-line" style="width: 30%;"></div>
                                <div class="declaracion-skeleton declaracion-skeleton-input"></div>
                            </div>
                        </div>
                        <div class="row mt-2">
                            <div class="col-sm">
                                <div class="declaracion-skeleton declaracion-skeleton-card"></div>
                            </div>
                            <div class="col-sm">
                                <div class="declaracion-skeleton declaracion-skeleton-card"></div>
                            </div>
                        </div>
                        <div class="declaracion-skeleton declaracion-skeleton-grid mt-2"></div>
                    </div>
                }
                else if (socioSeleccionado != null)
                {
                    <div class="row" style="width=100%;">
                        <div class="col-sm">
                            <label>Numero de Folio (opcional)</label>
                            <InputNumber class="form-control" @bind-Value="@oDeclaracion.NrFolio" placeholder="Folio" step="1" min="0" inputmode="numeric" pattern="[0-9]*" autocomplete="off" name="folio" autocapitalize="off" spellcheck="false"></InputNumber>
                        </div>
                        <div class="col-sm">
                            <label class="col-sm">Fecha de declaracion</label>
                            <InputDate TValue="DateTime?"
                                       @bind-Value="@oDeclaracion.Fchrecepcion"
                                       class="form-control w-75" />
                        </div>
                        <div class="col-sm">
                            <label class="col-sm">Plantel</label>
                            <BlazoredTypeahead Value="Plantelseleccionado"
                                               ValueChanged="OnPlantelSeleccionado"
                                               ValueExpression="@(() => Plantelseleccionado)"
                                               TItem="PlantelDTO"
                                               TValue="PlantelDTO"
                                               SearchMethod="BusquedaPlanteles"
                                               placeholder="Plantel - Año"
                                               EnableDropDown="true"
                                               Disabled="@(Id != 0)">
                                <SelectedTemplate Context="eleccion">
                                    @FormatoPlantel(eleccion)
                                </SelectedTemplate>
                                <ResultTemplate Context="eleccion">
                                    @FormatoPlantel(eleccion)
                                </ResultTemplate>
                            </BlazoredTypeahead>
                        </div>
                    </div>
                    <br />
                    @if (Plantelseleccionado != null)
                    {
                        <div class="row" style="width=100%">
                            <div class="col-sm">
                                <label>Cantidad de Vientres PR</label>
                                <div class="card">
                                    <div class="card-body">
                                        <a>@((Plantelseleccionado.Varede ?? 0) + (Plantelseleccionado.Vqssrd ?? 0) + (Plantelseleccionado.Vqcsrd ?? 0))</a>
                                    </div>
                                </div>
                            </div>
                            <div class="col-sm">
                                <label>Cantidad de Vientres VIP</label>
                                <div class="card">
                                    <div class="card-body">
                                        <a>@((Plantelseleccionado.Varepr ?? 0) + (Plantelseleccionado.Vqcsrp ?? 0) + (Plantelseleccionado.Vqssrp ?? 0))</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <br />
                    }
                    else
                    {
                        <div class="row" style="width=100%">
                            <div class="col-sm">
                                <label>Cantidad de Vientres PR</label>
                                <div class="card">
                                    <div class="card-body">
                                        <h5></h5>
                                    </div>
                                </div>
                            </div>
                            <div class="col-sm">
                                <label>Cantidad de Vientres VIP</label>
                                <div class="card">
                                    <div class="card-body">
                                        <h5></h5>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <br />
                    }
                    <div class="row" style="width=100%">
                        <div class="col-sm">
                            <label>Vientres de Servicio PR</label>
                            <RadzenNumeric @bind-Value="oDeclaracion.Cantv" class="w-100"></RadzenNumeric>
                        </div>
                        <div class="col-sm">
                            <label>Vientres de Servicio VIP</label>
                            <RadzenNumeric @bind-Value="oDeclaracion.Cantb" class="w-100"></RadzenNumeric>
                        </div>
                    </div>
                    <br />
                    <div class="row" style="width=100%">

                        <div class="col-sm">
                        </div>
                        <div class="col-sm">
                        </div>
                        <div class="col-sm">
                            <label>Tipo de servicio</label>
                            <RadzenDropDown @bind-Value="tipoTemp"
                                            Data="@tipos"
                                            Change="OnTipoChanged"
                                            Style="width: 100%; max-width: 400px;" />

                        </div>
                    </div>

                    <br />
                    @if (IsNaturalTipo(tipoTemp))
                    {
                        <label style="text-align:center">Periodo de servicio</label>
                        <div class="row">
                            <div class="col-sm">
                                <label>Desde</label>
                                <br />
                                <InputDate TValue="DateTime?"
                                           @bind-Value="@oDeclaracion.Desde"
                                           class="form-control w-75" />
                            </div>
                            <div class="col-sm">
                                <label>Hasta</label>
                                <br />
                                <InputDate TValue="DateTime?"
                                           @bind-Value="@oDeclaracion.Hasta"
                                           class="form-control w-75" />
                            </div>
                        </div>
                        <br />
                        <div class="declaracion-section">
                            <div class="declaracion-section-header">
                                <div class="declaracion-section-title">Toros seleccionados</div>
                                <div class="declaracion-selector">
                                    <label>Agregar toro</label>
                                    <BlazoredTypeahead SearchMethod="BusquedaToros"
                                                       placeholder="Buscar toro por nombre, HBA o tatuaje"
                                                       EnableDropDown="true"
                                                       TItem="TorosuniDTO"
                                                       TValue="TorosuniDTO"
                                                       Value="toroSeleccionado"
                                                       ValueChanged="OnToroSeleccionado"
                                                       ValueExpression="@(() => toroSeleccionado)">
                                        <SelectedTemplate Context="eleccion">
                                            @($"{DisplayOrNoInformado(eleccion?.NomDad)} - {DisplayOrNoInformado(eleccion?.Hba)}")
                                        </SelectedTemplate>
                                        <ResultTemplate Context="eleccion">
                                            @($"{DisplayOrNoInformado(eleccion?.NomDad)} | HBA {DisplayOrNoInformado(eleccion?.Hba)} | Tat {DisplayOrNoInformado(eleccion?.Tatpart)}")
                                        </ResultTemplate>
                                    </BlazoredTypeahead>
                                </div>
                            </div>
                            <RadzenDataGrid PageSize="25" Density="Density.Compact" Data="@torosSeleccionados" TItem="Desepla3Data" ColumnWidth="3.33%"
                                            EmptyText="Sin registros"
                                            LogicalFilterOperator="LogicalFilterOperator.Or" PagerHorizontalAlign="HorizontalAlign.Center" @ref="gridToro1">
                            <Columns>
                                <RadzenDataGridColumn TItem="Desepla3Data" Property="" Title="Nombre" Width="9%">
                                    <Template Context="data">
                                        @{
                                            var toro = toros.FirstOrDefault(x => string.Equals(T(x.Hba), T(data.original.Hardb), StringComparison.OrdinalIgnoreCase));
                                        }
                                        @DisplayOrNoInformado(toro?.NomDad)
                                    </Template>
                                </RadzenDataGridColumn>
                                <RadzenDataGridColumn TItem="Desepla3Data" Property="" Title="HBA" Width="4.6%">
                                    <Template Context="data">
                                        @DisplayOrNoInformado(data.original.Hardb)
                                    </Template>
                                </RadzenDataGridColumn>
                                <RadzenDataGridColumn TItem="Desepla3Data" Property="" Title="Tatuaje" Width="4.6%">
                                    <Template Context="data">
                                        @DisplayOrNoInformado(data.original.Tatpart)
                                    </Template>
                                </RadzenDataGridColumn>
                                <RadzenDataGridColumn TItem="Desepla3Data" Property="" Title="Tipo" Width="4.6%">
                                    <Template Context="data">
                                        @GetTipToroLabel(data.TipToro)
                                    </Template>
                                </RadzenDataGridColumn>
                                <RadzenDataGridColumn TItem="Desepla3Data" Title="Quitar" Width="3%">
                                    <Template Context="data">
                                        <button class="btn" type="button" title="Eliminar" @onclick="() => RemoveToro(data)"><FeatherTrash2 Size="16" Color="red" /></button>
                                    </Template>
                                </RadzenDataGridColumn>
                            </Columns>
                        </RadzenDataGrid>
                        </div>
                    }
                    @if (IsTipoConToros(tipoTemp))
                    {

                        <div class="declaracion-section">
                            <div class="declaracion-section-header">
                                <div class="declaracion-section-title">Toros seleccionados</div>
                                <div class="declaracion-selector">
                                    <label>Agregar toro</label>
                                    <BlazoredTypeahead SearchMethod="BusquedaToros"
                                                       placeholder="Buscar toro por nombre, HBA o tatuaje"
                                                       EnableDropDown="true"
                                                       TItem="TorosuniDTO"
                                                       TValue="TorosuniDTO"
                                                       Value="toroSeleccionado"
                                                       ValueChanged="OnToroSeleccionado"
                                                       ValueExpression="@(() => toroSeleccionado)">
                                        <SelectedTemplate Context="eleccion">
                                            @($"{DisplayOrNoInformado(eleccion?.NomDad)} - {DisplayOrNoInformado(eleccion?.Hba)}")
                                        </SelectedTemplate>
                                        <ResultTemplate Context="eleccion">
                                            @($"{DisplayOrNoInformado(eleccion?.NomDad)} | HBA {DisplayOrNoInformado(eleccion?.Hba)} | Tat {DisplayOrNoInformado(eleccion?.Tatpart)}")
                                        </ResultTemplate>
                                    </BlazoredTypeahead>
                                </div>
                            </div>
                            <RadzenDataGrid PageSize="25" Density="Density.Compact" Data="@torosSeleccionados" TItem="Desepla3Data" ColumnWidth="3.33%"
                                            EmptyText="Sin registros"
                                            LogicalFilterOperator="LogicalFilterOperator.Or" PagerHorizontalAlign="HorizontalAlign.Center" @ref="gridToro2">
                            <Columns>
                                <RadzenDataGridColumn TItem="Desepla3Data" Property="" Title="Nombre" Width="9%">
                                    <Template Context="data">
                                        @{
                                            var toro = toros.FirstOrDefault(x => string.Equals(T(x.Hba), T(data.original.Hardb), StringComparison.OrdinalIgnoreCase));
                                        }
                                        @DisplayOrNoInformado(toro?.NomDad)
                                    </Template>
                                </RadzenDataGridColumn>
                                <RadzenDataGridColumn TItem="Desepla3Data" Property="" Title="HBA" Width="3%">
                                    <Template Context="data">
                                        @DisplayOrNoInformado(data.original.Hardb)
                                    </Template>
                                </RadzenDataGridColumn>
                                <RadzenDataGridColumn TItem="Desepla3Data" Property="" Title="Tatuaje" Width="4.6%">
                                    <Template Context="data">
                                        @DisplayOrNoInformado(data.original.Tatpart)
                                    </Template>
                                </RadzenDataGridColumn>
                                <RadzenDataGridColumn TItem="Desepla3Data" Property="" Title="Tipo de toro" Width="5%">
                                    <Template Context="data">
                                        @GetTipToroLabel(data.TipToro)
                                    </Template>
                                </RadzenDataGridColumn>
                                <RadzenDataGridColumn TItem="Desepla3Data" Property="" Title="Desde" Width="7%">
                                    <Template Context="data">
                                        <RadzenDatePicker TValue="DateTime"
                                                          @bind-Value="@data.Desde"
                                                          DateFormat="dd/MM/yyyy"
                                                          InitialViewDate="@data.Desde"
                                                          YearRange="@CalendarYearRange"
                                                          CurrentDateChanged="@(currentDate => UpdateToroDesdeCurrentDate(data, currentDate))"
                                                          Style="width: 100%; min-width: 110px;" />
                                    </Template>
                                </RadzenDataGridColumn>
                                <RadzenDataGridColumn TItem="Desepla3Data" Property="" Title="Hasta" Width="7%">
                                    <Template Context="data">
                                        <RadzenDatePicker TValue="DateTime"
                                                          @bind-Value="@data.Hasta"
                                                          DateFormat="dd/MM/yyyy"
                                                          InitialViewDate="@data.Hasta"
                                                          YearRange="@CalendarYearRange"
                                                          CurrentDateChanged="@(currentDate => UpdateToroHastaCurrentDate(data, currentDate))"
                                                          Style="width: 100%; min-width: 110px;" />
                                    </Template>
                                </RadzenDataGridColumn>
                                <RadzenDataGridColumn TItem="Desepla3Data" Property="" Title="Cant Vientres" Width="5%">
                                    <Template Context="data">
                                        <RadzenNumeric @bind-Value="data.original.Cantv" Min="0" class="w-100"></RadzenNumeric>
                                    </Template>
                                </RadzenDataGridColumn>
                                <RadzenDataGridColumn TItem="Desepla3Data" Title="Quitar" Width="4%">
                                    <Template Context="data">
                                        <button class="btn" type="button" title="Eliminar" @onclick="() => RemoveToro(data)"><FeatherTrash2 Size="16" Color="red" /></button>
                                    </Template>
                                </RadzenDataGridColumn>
                            </Columns>
                        </RadzenDataGrid>
                        </div>
                    }
                    @if (IsTipoInseminacion(tipoTemp))
                    {
                        <div class="declaracion-section">
                            <div class="declaracion-section-header">
                                <div class="declaracion-section-title">Dosis seleccionadas</div>
                                <div class="declaracion-selector">
                                    <label>Agregar dosis</label>
                                    <BlazoredTypeahead SearchMethod="BusquedaCertificados"
                                                       placeholder="Buscar dosis por nombre, HBA o tatuaje"
                                                       EnableDropDown="true"
                                                       TItem="CertifsemanDTO"
                                                       TValue="CertifsemanDTO"
                                                       Value="certifSeleccionado"
                                                       ValueChanged="OnCertifSeleccionado"
                                                       ValueExpression="@(() => certifSeleccionado)">
                                        <SelectedTemplate Context="eleccion">
                                            @($"{eleccion?.NomDad} - {eleccion?.Hba}")
                                        </SelectedTemplate>
                                        <ResultTemplate Context="eleccion">
                                            @($"{eleccion?.NomDad} | HBA {eleccion?.Hba} | Tat {eleccion?.Tatpart} | Dosis {eleccion?.NrDosi}")
                                        </ResultTemplate>
                                    </BlazoredTypeahead>
                                </div>
                            </div>
                            <RadzenDataGrid PageSize="25" Density="Density.Compact" Data="@certificadosSeleccionados" TItem="Desepla3Data" ColumnWidth="3.33%"
                                            EmptyText="Sin registros"
                                            LogicalFilterOperator="LogicalFilterOperator.Or" PagerHorizontalAlign="HorizontalAlign.Center" @ref="gridCertif">
                            <Columns>
                                <RadzenDataGridColumn TItem="Desepla3Data" Property="" Title="Nombre" Width="9%">
                                    <Template Context="data">
                                        @{
                                            var cert = certificados.FirstOrDefault(x => string.Equals(T(x.Hba), T(data.original.Hardb), StringComparison.OrdinalIgnoreCase));
                                        }
                                        @(cert?.NomDad ?? "-")
                                    </Template>
                                </RadzenDataGridColumn>
                                <RadzenDataGridColumn TItem="Desepla3Data" Property="" Title="HBA" Width="7.34%">
                                    <Template Context="data">
                                        @data.original.Hardb
                                    </Template>
                                </RadzenDataGridColumn>
                                <RadzenDataGridColumn TItem="Desepla3Data" Property="" Title="Tatuaje" Width="7%">
                                    <Template Context="data">
                                        @data.original.Tatpart
                                    </Template>
                                </RadzenDataGridColumn>

                                <RadzenDataGridColumn TItem="Desepla3Data" Title="Dosis Reman" Width="5%">
                                    <Template Context="data">
                                        @GetDosisRemanDisplay(data?.original?.Hardb)
                                    </Template>
                                </RadzenDataGridColumn>

                                <RadzenDataGridColumn TItem="Desepla3Data" Property="" Title="Vientres" Width="6%">
                                    <Template Context="data">
                                        <RadzenNumeric @bind-Value="data.original.Cantv" class="w-100"></RadzenNumeric>
                                    </Template>
                                </RadzenDataGridColumn>

                                <RadzenDataGridColumn TItem="Desepla3Data" Property="" Title="Dosis Necesaria" Width="5%">
                                    <Template Context="data">
                                        @if (data.original.Cantv != null)
                                        {
                                            @switch (tipoTemp)
                                            {
                                                case "Inseminacion c/celo":
                                                    <a>@Math.Round((decimal)(data.original.Cantv * 1.5))</a>
                                                    break;
                                                case "Inseminacion c/celo y rep":
                                                case "IATF c/rep":
                                                    <a>@data.original.Cantv</a>
                                                    break;
                                                case "Inseminacion c/p y rep":
                                                    <a>@Math.Round((decimal)(data.original.Cantv * 0.8))</a>
                                                    break;
                                            }
                                        }
                                        else
                                        {
                                            <a>0</a>
                                        }
                                    </Template>
                                </RadzenDataGridColumn>

                                <RadzenDataGridColumn TItem="Desepla3Data" Title="Desde" Width="9%">
                                    <Template Context="data">
                                        <RadzenDatePicker TValue="DateTime"
                                                          @bind-Value="data.Desde"
                                                          DateFormat="dd/MM/yyyy"
                                                          InitialViewDate="@data.Desde"
                                                          YearRange="@CalendarYearRange"
                                                          CurrentDateChanged="@(currentDate => UpdateCertifDesdeCurrentDate(data, currentDate))"
                                                          Style="width: 100%; min-width: 110px;" />
                                    </Template>
                                </RadzenDataGridColumn>

                                <RadzenDataGridColumn TItem="Desepla3Data" Title="Hasta" Width="9%">
                                    <Template Context="data">
                                        <RadzenDatePicker TValue="DateTime"
                                                          @bind-Value="data.Hasta"
                                                          DateFormat="dd/MM/yyyy"
                                                          InitialViewDate="@data.Hasta"
                                                          YearRange="@CalendarYearRange"
                                                          CurrentDateChanged="@(currentDate => UpdateCertifHastaCurrentDate(data, currentDate))"
                                                          Style="width: 100%; min-width: 110px;" />
                                    </Template>
                                </RadzenDataGridColumn>

                                <RadzenDataGridColumn TItem="Desepla3Data" Title="Quitar" Width="4%">
                                    <Template Context="data">
                                        <button class="btn" type="button" title="Eliminar" @onclick="() => RemoveCertif(data)">
                                            <FeatherTrash2 Size="16" Color="red" />
                                        </button>
                                    </Template>
                                </RadzenDataGridColumn>
                            </Columns>
                        </RadzenDataGrid>
                        </div>
                        <br />
                    }
                    <br />


                }
            </EditForm>
        </div>
    </div>
}
else
{
    if (esSocio && socioSeleccionado == null)
    {
        <div class="alert alert-danger text-center mt-4">
            <h4>No se encontró un socio asociado a esta cuenta.</h4>
            <p>Por favor, cree un socio para poder continuar.</p>
        </div>
    }
    else
    {
        <br />
        <center>
            <div class="mb-2">
                <br />
                <RadzenProgressBar style="position:relative;width:93.5%"
                                   Value="100"
                                   ShowValue="false"
                                   ProgressBarStyle="ProgressBarStyle.Primary"
                                   Mode="ProgressBarMode.Indeterminate" />
            </div>
        </center>
    }
}

@code {
    Desepla1DTO oDeclaracion = new();
    List<SocioDTO> socios = new();
    List<PlantelDTO> Planteles = new();
    List<Desepla3DTO> seleccionFinal = new();
    RadzenDataGrid<Desepla3Data> gridToro1;
    RadzenDataGrid<Desepla3Data> gridToro2;
    RadzenDataGrid<Desepla3Data> gridCertif;

    [Parameter] public int Id { get; set; }


    bool esSocio = false;
    bool _isEdit => Id != 0;
    bool isInitialLoading = true;
    bool isLoadingDependencias = false;
    string loadingMessage = "Preparando declaracion...";

    public string nrodec = "";
    SocioDTO? socioSeleccionado = null;
    PlantelDTO? Plantelseleccionado = null;
    TorosuniDTO? toroSeleccionado = null;
    CertifsemanDTO? certifSeleccionado = null;
    List<string> tipos = new List<string> { "Natural", "Natural a Corral", "Inseminacion c/celo", "Inseminacion c/celo y rep", "Inseminacion c/p y rep", "IATF c/rep" };
    List<TorosuniDTO> toros = new();
    List<Desepla3Data> torosSeleccionados = new();
    List<CertifsemanDTO> certificados = new();
    List<Desepla3Data> certificadosSeleccionados = new();
    List<Desepla3DTO> deseplasViejos = new();
    string tipoTemp = "";
    string previousTipoTemp = "";
    string? plantelLoadWarning = null;
    private const string NoInformado = "No informado";

    Dictionary<string, string> traductorToros = new Dictionary<string, string>(StringComparer.OrdinalIgnoreCase){
        {"P","Puro Pedigree (SRA)"},
        {"S","S/"},
        {"GP","Ganador de prueba"},
        {"A","Patagonico"},
        {"1","Activo"},
        {"2","Inactivo"},
        {"3","Inexistente"},
        {"4","Muerto"},
        {"5","Vendido"}
    };
    private ClaimsPrincipal? user;
    private const string CalendarYearRange = "1900:2100";

    private const string LogPrefix = "[AddDeclaracion]";
    private void Log(string message) => Console.WriteLine($"{LogPrefix} {DateTime.Now:HH:mm:ss.fff} {message}");
    private static string Q(string? value) => value is null ? "<null>" : $"'{value}' (len={value.Length})";
    private static string T(string? value) => (value ?? string.Empty).Trim();
    private static string DisplayOrNoInformado(string? value) => string.IsNullOrWhiteSpace(value) ? NoInformado : value.Trim();
    private static string EscapeForFilter(string? value) => (value ?? string.Empty).Replace("\"", "\\\"");
    private static DateTime KeepDayWithinMonth(DateTime current, DateTime selected)
    {
        var year = selected.Year;
        var month = selected.Month;
        var day = Math.Min(current.Day, DateTime.DaysInMonth(year, month));
        return new DateTime(year, month, day, current.Hour, current.Minute, current.Second, current.Kind);
    }

    private void UpdateToroDesdeCurrentDate(Desepla3Data data, DateTime currentDate) => data.Desde = KeepDayWithinMonth(data.Desde, currentDate);
    private void UpdateToroHastaCurrentDate(Desepla3Data data, DateTime currentDate) => data.Hasta = KeepDayWithinMonth(data.Hasta, currentDate);
    private void UpdateCertifDesdeCurrentDate(Desepla3Data data, DateTime currentDate) => data.Desde = KeepDayWithinMonth(data.Desde, currentDate);
    private void UpdateCertifHastaCurrentDate(Desepla3Data data, DateTime currentDate) => data.Hasta = KeepDayWithinMonth(data.Hasta, currentDate);
    private void LogDetalleSnapshot(string title, IEnumerable<Desepla3DTO>? detalles)
    {
        var list = detalles?.ToList() ?? new List<Desepla3DTO>();
        var resumen = string.Join(" | ", list.Take(12).Select(d =>
            $"Id={d.Id},Tipo={Q(d.Tipo)},HBA={Q(d.Hardb)},Tat={Q(d.Tatpart)},Cantv={d.Cantv},Desde={Q(d.Desde)},Hasta={Q(d.Hasta)}"));
        Log($"{title} | total={list.Count} | {resumen}");
    }

    private void LogSelectedSnapshot(string title, IEnumerable<Desepla3Data>? items)
    {
        var list = items?.ToList() ?? new List<Desepla3Data>();
        var resumen = string.Join(" | ", list.Take(12).Select(d =>
            $"Line={d.LineKey},Tipo={Q(d.original?.Tipo)},HBA={Q(d.original?.Hardb)},Tat={Q(d.original?.Tatpart)},Cantv={d.original?.Cantv},Desde={d.Desde:yyyy-MM-dd},Hasta={d.Hasta:yyyy-MM-dd}"));
        Log($"{title} | total={list.Count} | {resumen}");
    }

    private (DateTime Desde, DateTime Hasta)? GetPeriodoSeleccionado()
    {
        var items = torosSeleccionados.Concat(certificadosSeleccionados).ToList();
        if (items.Count == 0)
            return null;

        return (
            items.Min(x => x.Desde),
            items.Max(x => x.Hasta)
        );
    }


    Desepla3DTO CloneDesepla(Desepla3DTO s) => new Desepla3DTO
        {
            Id = s.Id,
            Tatpart = s.Tatpart,
            Hardb = s.Hardb,
            Cantv = s.Cantv,
            Nrodec = s.Nrodec,
            Tipo = s.Tipo,
            Servicio = s.Servicio,
            Desde = s.Desde,
            Hasta = s.Hasta,
            Apodo = s.Apodo
        };



    protected override async Task OnInitializedAsync()
    {
        Log($"OnInitializedAsync START | Id={Id}");
        loadingMessage = Id != 0 ? "Cargando declaracion y datos asociados..." : "Preparando declaracion...";

        try
        {
            torosSeleccionados.Clear();
            certificadosSeleccionados.Clear();
            seleccionFinal.Clear();
            deseplasViejos.Clear();

            var authState = await AuthenticationStateProvider.GetAuthenticationStateAsync();
            user = authState.User;
            esSocio = await UserHasRole("Socio");
            Log($"Usuario autenticado | esSocio={esSocio} | claims={user?.Claims?.Count() ?? 0}");

            if (esSocio)
            {
                var socioId = await CurrentUserHelper.GetCurrentSocioIdAsync(AuthenticationStateProvider, http);
                Log($"SocioId actual desde backend={Q(socioId?.ToString())}");
                if (socioId.HasValue)
                {
                    string filtro = $"Id == {socioId.Value}";
                    Log($"Buscando socio por claim con filtro: {filtro}");

                    var socio = await _socioServicio.LimitadosFiltrados(0, 0, filtro);
                    socioSeleccionado = socio.List.FirstOrDefault();
                    socios = socioSeleccionado != null ? new List<SocioDTO> { socioSeleccionado } : new List<SocioDTO>();
                    Log($"Resultado socio por claim | count={socio.List?.Count ?? 0} | seleccionado.Id={socioSeleccionado?.Id} | seleccionado.Scod={Q(socioSeleccionado?.Scod)}");

                    if (socioSeleccionado == null)
                    {
                        Log("No se encontró socio para el usuario actual (claim SocioId).");
                    }
                }
                else
                {
                    Log("No se pudo obtener SocioId actualizado.");
                }
            }
            else
            {
                var res = await _socioServicio.LimitadosFiltrados(0, 0);
                socios = res.List;
                Log($"Modo admin/maestro | socios disponibles: {socios?.Count ?? 0}");
            }

            Log($"Socios disponibles en pantalla: {socios?.Count ?? 0}");

            if (Id != 0)
            {
                Log($"Entrando en edición | buscando cabecera Id={Id}");
                var rtaD = await _desepla1Servicio.LimitadosFiltradosNoInclude(0, 1, $"Id = {Id}");
                oDeclaracion = rtaD.List.FirstOrDefault() ?? new();
                Log($"Cabecera cargada | Nrodec={Q(oDeclaracion.Nrodec)} | Nrocri={Q(oDeclaracion.Nrocri)} | Nroplan={Q(oDeclaracion.Nroplan)} | Tipse={Q(oDeclaracion.Tipse)}");

                nrodec = oDeclaracion.Nrodec;
                socioSeleccionado = socios.FirstOrDefault(x => x.Scod == oDeclaracion.Nrocri);
                Log($"Match socio por Scod exacto | encontrado={(socioSeleccionado != null)}");
                if (socioSeleccionado is null)
                {
                    var nrocriTrim = T(oDeclaracion.Nrocri);
                    var socioTrim = socios.FirstOrDefault(x => T(x.Scod).Equals(nrocriTrim, StringComparison.OrdinalIgnoreCase));
                    Log($"Match socio por Scod trim/case-insensitive | encontrado={(socioTrim != null)} | scodBuscado={Q(oDeclaracion.Nrocri)}");
                    socioSeleccionado = socioTrim;
                }

                Task<Respuesta<List<Desepla3DTO>>>? detallesTask = null;
                if (!string.IsNullOrWhiteSpace(oDeclaracion.Nrodec))
                {
                    Log($"Buscando detalles Desepla3 por Nrodec={Q(oDeclaracion.Nrodec)}");
                    detallesTask = _desepla3Servicio.GetByNrodec(oDeclaracion.Nrodec ?? string.Empty);
                }

                if (socioSeleccionado != null)
                {
                    await LoadSocioDataAsync(socioSeleccionado, preserveCabecera: true, refreshUi: false);
                }
                else
                {
                    Log("No se pudieron cargar listas dependientes (socioSeleccionado == null).");
                }

                var plantelResuelto = ResolvePlantelForDeclaracion(
                    oDeclaracion.Nroplan,
                    oDeclaracion.Fchrecepcion ?? oDeclaracion.Fecdecl,
                    out var plantelWarning);
                plantelLoadWarning = plantelWarning;
                Plantelseleccionado = string.IsNullOrWhiteSpace(plantelLoadWarning) ? plantelResuelto : null;
                Log($"Plantel reconstruido | encontrado={(Plantelseleccionado != null)} | warning={Q(plantelLoadWarning)} | totalPlanteles={Planteles?.Count ?? 0}");
                if (Plantelseleccionado != null && !Planteles.Any(p => p.Id == Plantelseleccionado.Id))
                {
                    Planteles.Add(Plantelseleccionado);
                    Planteles = OrderPlantelesByYear(Planteles);
                    Log($"Plantel reconstruido agregado a lista local | plantelesTotal={Planteles.Count}");
                }
                tipoTemp = ToLabel(oDeclaracion.Tipse);
                previousTipoTemp = tipoTemp;
                Log($"Tipo reconstruido | tipseDB={Q(oDeclaracion.Tipse)} -> tipoUI={Q(tipoTemp)}");

                if (detallesTask != null)
                {
                    var rt = await detallesTask;
                    deseplasViejos = rt.List ?? new();
                }
                else
                {
                    deseplasViejos = new List<Desepla3DTO>();
                }
                Log($"Detalles cargados: {deseplasViejos.Count}");
                LogDetalleSnapshot("Detalles crudos desde API", deseplasViejos);

                torosSeleccionados.Clear();
                certificadosSeleccionados.Clear();

                foreach (var desepla in deseplasViejos)
                {
                    var data = new Desepla3Data
                        {
                            LineKey = Guid.NewGuid(),
                            original = CloneDesepla(desepla),
                            Id = desepla.Id
                        };

                    if (DateTime.TryParseExact(desepla.Desde, "yyyy-MM-dd", null, System.Globalization.DateTimeStyles.None, out var d1))
                        data.Desde = d1;
                    if (DateTime.TryParseExact(desepla.Hasta, "yyyy-MM-dd", null, System.Globalization.DateTimeStyles.None, out var d2))
                        data.Hasta = d2;

                    if (string.Equals(desepla.Tipo, "insem", StringComparison.OrdinalIgnoreCase))
                    {
                        certificadosSeleccionados.Add(data);
                    }
                    else
                    {
                        data.TipToro = GetTipToroByHba(desepla.Hardb);
                        data.SelectionKey = BuildToroSelectionKey(data);
                        torosSeleccionados.Add(data);
                    }
                }
                Log($"Reconstrucción listas seleccionadas | toros={torosSeleccionados.Count} | certifs={certificadosSeleccionados.Count}");
                LogSelectedSnapshot("Reconstruccion toros seleccionados", torosSeleccionados);
                LogSelectedSnapshot("Reconstruccion certificados seleccionados", certificadosSeleccionados);
                Log("Edición inicializada.");
            }
            else
            {
                oDeclaracion.Fecdecl = DateTime.Today;
                nrodec = "0";
                oDeclaracion.Nrodec = nrodec;
                oDeclaracion.Desde = DateTime.Today;
                oDeclaracion.Hasta = DateTime.Today;
                oDeclaracion.Tipse = "NA";
                Log($"Alta nueva | nrodecTemporal={Q(nrodec)} | el servidor asignara el nrodec definitivo al guardar");

                if (socioSeleccionado != null)
                {
                    await LoadSocioDataAsync(socioSeleccionado, preserveCabecera: false, refreshUi: false);
                }
            }
        }
        finally
        {
            isLoadingDependencias = false;
            isInitialLoading = false;
            await InvokeAsync(StateHasChanged);
            Log("OnInitializedAsync END");
        }
    }

    private string? GetTipToroByHba(string? hba)
    {
        if (string.IsNullOrWhiteSpace(hba)) return null;
        var normalizedHba = T(hba);
        return toros.FirstOrDefault(t => string.Equals(T(t.Hba), normalizedHba, StringComparison.OrdinalIgnoreCase))?.TipToro;
    }

    private string GetTipToroLabel(string? tipToro)
    {
        if (string.IsNullOrWhiteSpace(tipToro))
        {
            return NoInformado;
        }

        return traductorToros.TryGetValue(tipToro.Trim(), out var label)
            ? label
            : tipToro.Trim();
    }

    private static string BuildToroSelectionKey(TorosuniDTO? toro)
    {
        if (toro is null)
        {
            return string.Empty;
        }

        if (toro.Id > 0)
        {
            return $"ID:{toro.Id}";
        }

        var hba = T(toro.Hba);
        if (!string.IsNullOrWhiteSpace(hba))
        {
            return $"HBA:{hba.ToUpperInvariant()}";
        }

        var nomDad = T(toro.NomDad);
        var tatpart = T(toro.Tatpart);
        return $"BUS:{nomDad.ToUpperInvariant()}|{tatpart.ToUpperInvariant()}";
    }

    private static string BuildToroSelectionKey(Desepla3Data? data)
    {
        if (data is null)
        {
            return string.Empty;
        }

        if (!string.IsNullOrWhiteSpace(data.SelectionKey))
        {
            return data.SelectionKey.Trim();
        }

        var hba = T(data.original?.Hardb);
        if (!string.IsNullOrWhiteSpace(hba))
        {
            return $"HBA:{hba.ToUpperInvariant()}";
        }

        if (data.Id > 0)
        {
            return $"DET:{data.Id}";
        }

        var apodo = T(data.original?.Apodo);
        var tatpart = T(data.original?.Tatpart);
        return $"BUS:{apodo.ToUpperInvariant()}|{tatpart.ToUpperInvariant()}";
    }

    private async Task<bool> UserHasRole(string role)
    {
        var authenticationState = await AuthenticationStateProvider.GetAuthenticationStateAsync();
        return authenticationState.User.IsInRole(role);
    }
    async Task GenerarOtra()
    {
        await Guardar();
        Navigation.NavigateTo(Navigation.Uri, forceLoad: true);
    }






    double GetMultiplicador(string tipo) => DeclaracionServicioHelper.GetMultiplicador(tipo);

    int DosisNecesarias(double? cantVientres, double multiplicador)
    {
        var v = cantVientres ?? 0.0;
        if (multiplicador <= 0) return 0;

        return (int)Math.Round(v * multiplicador, MidpointRounding.AwayFromZero);
    }
    Dictionary<string, string> errors = new();
    Dictionary<string, string> warnings = new();

    // Construye un mapa HBA -> dosis totales usadas en la declaración
    Dictionary<string, int> BuildUsoPorCert(IEnumerable<Desepla3DTO> items, double multiplicador)
    {
        return items
            .Where(x => IsDetalleInseminacion(x.Tipo))
            .GroupBy(x => (x.Hardb ?? string.Empty).Trim())
            .ToDictionary(
                g => g.Key,
                g => g.Sum(d => DosisNecesarias(d.Cantv, multiplicador))
            );
    }

    Dictionary<string, int> BuildUsoPorCertFromSelected(IEnumerable<Desepla3Data> items, double multiplicador)
    {
        return items
            .Where(x => x.original != null && IsDetalleInseminacion(x.original.Tipo) && !string.IsNullOrWhiteSpace(x.original.Hardb))
            .GroupBy(x => (x.original.Hardb ?? string.Empty).Trim())
            .ToDictionary(
                g => g.Key,
                g => g.Sum(d => DosisNecesarias(d.original.Cantv, multiplicador))
            );
    }

    private int GetDosisRemanDisplay(string? hba)
    {
        if (string.IsNullOrWhiteSpace(hba)) return 0;

        var cert = certificados.FirstOrDefault(x => string.Equals(T(x.Hba), T(hba), StringComparison.OrdinalIgnoreCase));
        var baseStock = cert?.NrDosi ?? 0;

        // Si estamos editando, devolvemos al stock lo usado originalmente por la declaración
        if (_isEdit && deseplasViejos.Count > 0)
        {
            var multPrev = GetMultiplicador(string.IsNullOrWhiteSpace(previousTipoTemp) ? tipoTemp : previousTipoTemp);
            var usoInicial = BuildUsoPorCert(deseplasViejos, multPrev);
            if (usoInicial.TryGetValue(hba, out var u))
            {
                baseStock += u;
            }
        }

        // Restamos lo que se está usando ahora en pantalla
        var multActual = GetMultiplicador(tipoTemp);
        var usoActual = BuildUsoPorCertFromSelected(certificadosSeleccionados, multActual);
        if (usoActual.TryGetValue(hba, out var a))
        {
            baseStock -= a;
        }

        return baseStock < 0 ? 0 : baseStock;
    }

    private string CampoLegible(string key) => key switch
    {
        "socio" => "Socio",
        "plantel" => "Plantel",
        "plantelCodigo" => "Código de plantel",
        "tipo" => "Tipo de servicio",
        "tipoCodigo" => "Tipo de servicio",
        "NrFolio" => "Nº de folio",
        "Fchrecepcion" => "Fecha de declaración",
        "cantv" => "Vientres PR",
        "cantb" => "Vientres VIP",
        "periodoNatural" => "Período (Natural)",
        "torosNatural" => "Toros (Natural)",
        "torosCorral" => "Toros (Natural a Corral)",
        "certifs" => "Certificados de semen",
        _ => key
    };


    private string ToCode(string label) => DeclaracionServicioHelper.ToCode(label);

    private string ToLabel(string code) => DeclaracionServicioHelper.ToLabel(code);

    private string NormalizeTipoLabel(string? label) => DeclaracionServicioHelper.NormalizeTipoLabel(label);

    private bool IsNaturalTipo(string? tipo) => DeclaracionServicioHelper.IsNaturalTipo(tipo);
    private bool IsNaturalCorralTipo(string? tipo) => DeclaracionServicioHelper.IsNaturalCorralTipo(tipo);
    private bool IsTipoInseminacion(string? tipo) => DeclaracionServicioHelper.IsTipoInseminacion(tipo);
    private bool IsTipoConToros(string? tipo) => DeclaracionServicioHelper.IsTipoConToros(tipo);



    private string BuildErrorsHtml()
    {
        var items = errors
            .Select(kv => $"<li><b>{CampoLegible(kv.Key)}:</b> {System.Net.WebUtility.HtmlEncode(kv.Value)}</li>");
        return $"<ul style='text-align:left;margin-left:1rem'>{string.Join("", items)}</ul>";
    }

    private string BuildWarningsHtml()
    {
        var items = warnings
            .Select(kv => $"<li><b>{CampoLegible(kv.Key)}:</b> {System.Net.WebUtility.HtmlEncode(kv.Value)}</li>");
        return $"<div style='margin-top:.5rem'><b>Advertencias:</b><ul style='text-align:left;margin-left:1rem'>{string.Join("", items)}</ul></div>";
    }

    private string FormatQty(double value) => Math.Abs(value - Math.Round(value)) < 0.0001
        ? Math.Round(value).ToString()
        : value.ToString("0.##");

    private string BuildPlantelCapacityDetail(string label, double vacas, double conServicio, double sinServicio, double total, double declarado)
    {
        return $"{label}: ingresaste {FormatQty(declarado)} y la capacidad del plantel es {FormatQty(total)} ({FormatQty(vacas)} + {FormatQty(conServicio)} + {FormatQty(sinServicio)}).";
    }

    private string BuildSaveHintsHtml()
    {
        var hints = new List<string>();

        if (Plantelseleccionado != null && !string.IsNullOrWhiteSpace(Plantelseleccionado.Placod) && Plantelseleccionado.Placod.Length > 6)
            hints.Add($"El código de plantel \"{System.Net.WebUtility.HtmlEncode(Plantelseleccionado.Placod)}\" supera 6 caracteres.");

        var tipseCode = string.IsNullOrWhiteSpace(tipoTemp) ? string.Empty : ToCode(tipoTemp);
        if (!string.IsNullOrWhiteSpace(tipseCode) && tipseCode.Length > 5)
            hints.Add($"El tipo de servicio seleccionado se envía como \"{System.Net.WebUtility.HtmlEncode(tipseCode)}\" y supera 5 caracteres.");

        if (hints.Count == 0) return string.Empty;

        var li = string.Join("", hints.Select(h => $"<li>{h}</li>"));
        return $"<div style='margin-top:.5rem'><b>Posibles causas:</b><ul style='text-align:left;margin-left:1rem'>{li}</ul></div>";
    }

    private async Task HandleSubmit(EditContext _)
    {
        Log($"HandleSubmit | socio={Q(socioSeleccionado?.Scod)} | plantel={Q(Plantelseleccionado?.Placod)} | tipo={Q(tipoTemp)} | torosSel={torosSeleccionados.Count} | certifsSel={certificadosSeleccionados.Count}");
        if (!ValidateManual())
        {
            Log($"ValidateManual FAIL | errores={string.Join(" | ", errors.Select(e => $"{e.Key}:{e.Value}"))}");
            // Alerta con resumen de errores
            await Swal.FireAsync(new SweetAlertOptions
                {
                    Title = "Faltan datos para guardar",
                    Html = BuildErrorsHtml(),
                    Icon = SweetAlertIcon.Warning,
                    ConfirmButtonText = "Revisar"
                });

            StateHasChanged(); // por si además mostrás mensajes rojos debajo
            return;
        }

        if (warnings.Count > 0)
        {
            Log($"ValidateManual WARN | advertencias={string.Join(" | ", warnings.Select(e => $"{e.Key}:{e.Value}"))}");
            var confirmContinue = await Swal.FireAsync(new SweetAlertOptions
            {
                Title = "¿Desea continuar?",
                Html = BuildWarningsHtml() + "<div style='margin-top:.75rem'>La cantidad de vientres en servicio es mayor a la declarada en el plantel.</div>",
                Icon = SweetAlertIcon.Warning,
                ShowCancelButton = true,
                ConfirmButtonText = "Sí, continuar",
                CancelButtonText = "Cancelar"
            });

            if (confirmContinue.Dismiss == DismissReason.Cancel)
            {
                return;
            }
        }

        Log("ValidateManual OK. Ejecutando Guardar...");
        await Guardar();
    }

    private void ClearErrors() => errors.Clear();
    private void ClearWarnings() => warnings.Clear();


    private bool ValidateManual()
    {
        ClearErrors();
        ClearWarnings();

        // 1) Requeridos básicos
        if (socioSeleccionado is null)
            errors["socio"] = "Debe seleccionar un socio.";

        if (Plantelseleccionado is null)
            errors["plantel"] = "Debe seleccionar un plantel.";
        else if (!string.IsNullOrWhiteSpace(Plantelseleccionado.Placod) && Plantelseleccionado.Placod.Length > 6)
            errors["plantelCodigo"] = "El código de plantel supera 6 caracteres y no puede guardarse.";

        if (string.IsNullOrWhiteSpace(tipoTemp))
            errors["tipo"] = "Seleccione el tipo de servicio.";
        else
        {
            var tipseCode = ToCode(tipoTemp);
            if (!string.IsNullOrWhiteSpace(tipseCode) && tipseCode.Length > 5)
                errors["tipoCodigo"] = "Tipo de servicio inválido para guardar. Intente re-seleccionarlo.";
        }

        if (oDeclaracion.Fchrecepcion is null)
            errors["Fchrecepcion"] = "Ingrese la fecha de declaración.";

        // 2) Límites PR/VIP contra el plantel
        var prTotal = (Plantelseleccionado?.Varede ?? 0)
                    + (Plantelseleccionado?.Vqssrd ?? 0)
                    + (Plantelseleccionado?.Vqcsrd ?? 0);

        var vipTotal = (Plantelseleccionado?.Varepr ?? 0)
                     + (Plantelseleccionado?.Vqssrp ?? 0)
                     + (Plantelseleccionado?.Vqcsrp ?? 0);

        if ((oDeclaracion.Cantv ?? 0) < 0)
            errors["cantv"] = "La cantidad de vientres de servicio PR no puede ser negativa.";
        else if ((oDeclaracion.Cantv ?? 0) > prTotal)
            warnings["cantv"] = BuildPlantelCapacityDetail(
                "Vientres de Servicio PR",
                Plantelseleccionado?.Varede ?? 0,
                Plantelseleccionado?.Vqcsrd ?? 0,
                Plantelseleccionado?.Vqssrd ?? 0,
                prTotal,
                oDeclaracion.Cantv ?? 0) + " Se guardará igual.";

        if ((oDeclaracion.Cantb ?? 0) < 0)
            errors["cantb"] = "La cantidad de vientres de servicio VIP no puede ser negativa.";
        else if ((oDeclaracion.Cantb ?? 0) > vipTotal)
            warnings["cantb"] = BuildPlantelCapacityDetail(
                "Vientres de Servicio VIP",
                Plantelseleccionado?.Varepr ?? 0,
                Plantelseleccionado?.Vqcsrp ?? 0,
                Plantelseleccionado?.Vqssrp ?? 0,
                vipTotal,
                oDeclaracion.Cantb ?? 0) + " Se guardará igual.";

        // 3) Reglas por tipo
        bool esNatural = IsNaturalTipo(tipoTemp);
        bool esNaturalCorral = IsNaturalCorralTipo(tipoTemp);
        bool esAlgunaInsemin = IsTipoInseminacion(tipoTemp);

        // Natural: requiere periodo y al menos 1 toro
        if (esNatural)
        {
            if (oDeclaracion.Desde == null || oDeclaracion.Hasta == null || oDeclaracion.Desde > oDeclaracion.Hasta)
                errors["periodoNatural"] = "Complete un periodo válido (Desde/Hasta).";

            if (torosSeleccionados.Count == 0)
                errors["torosNatural"] = "Debe seleccionar al menos un toro.";
        }

        // Natural a Corral: toros con fechas por fila
        if (esNaturalCorral)
        {
            bool algunaFilaInvalida = torosSeleccionados.Any(t => t.Desde == default || t.Hasta == default || t.Desde > t.Hasta);
            if (torosSeleccionados.Count == 0 || algunaFilaInvalida)
                errors["torosCorral"] = "Seleccione toros y complete fechas válidas en cada fila.";
        }

        // Inseminación: certificados con Cantv > 0 y fechas válidas
        if (esAlgunaInsemin)
        {
            bool faltan = certificadosSeleccionados.Count == 0;
            bool cantErr = certificadosSeleccionados.Any(c => (c.original.Cantv ?? 0) <= 0);
            bool fechasErr = certificadosSeleccionados.Any(c => c.Desde == default || c.Hasta == default || c.Desde > c.Hasta);

            if (faltan || cantErr || fechasErr)
                errors["certifs"] = "Agregue certificados, con vientres mayores a 0 y fechas válidas por fila.";
        }

        return errors.Count == 0;
    }
    async Task Guardar()
    {
        Log($"Guardar START | Id={Id} | nrodec={Q(oDeclaracion?.Nrodec)}");
        seleccionFinal.Clear();
        LogSelectedSnapshot("Guardar -> torosSeleccionados antes de persistir", torosSeleccionados);
        LogSelectedSnapshot("Guardar -> certificadosSeleccionados antes de persistir", certificadosSeleccionados);

        oDeclaracion.Nrocri = socioSeleccionado.Scod;
        oDeclaracion.Nroplan = Plantelseleccionado.Placod;
        oDeclaracion.Tipse = ToCode(tipoTemp);
        Log($"Cabecera lista para persistir | Nrocri={Q(oDeclaracion.Nrocri)} | Nroplan={Q(oDeclaracion.Nroplan)} | Tipse={Q(oDeclaracion.Tipse)}");


        // --- Multiplicadores
        var multActual = GetMultiplicador(tipoTemp);
        var multPrevio = GetMultiplicador(string.IsNullOrWhiteSpace(previousTipoTemp) ? tipoTemp : previousTipoTemp);
        Log($"Multiplicadores | actual={multActual} (tipo={Q(tipoTemp)}) | previo={multPrevio} (tipoPrev={Q(previousTipoTemp)})");



        if (IsNaturalTipo(tipoTemp))
        {
            var d = oDeclaracion.Desde ?? DateTime.Today;
            var h = oDeclaracion.Hasta ?? DateTime.Today;

            foreach (var t in torosSeleccionados)
            {
                t.Desde = d;
                t.Hasta = h;
            }
        }

        // --- Armo seleccionFinal como hacías
        foreach (var adaptacion in torosSeleccionados)
        {
            adaptacion.original.Desde = adaptacion.Desde.ToString("yyyy-MM-dd");
            adaptacion.original.Hasta = adaptacion.Hasta.ToString("yyyy-MM-dd");
            seleccionFinal.Add(adaptacion.original);
        }

        foreach (var adaptacion in certificadosSeleccionados)
        {
            adaptacion.original.Desde = adaptacion.Desde.ToString("yyyy-MM-dd");
            adaptacion.original.Hasta = adaptacion.Hasta.ToString("yyyy-MM-dd");
            seleccionFinal.Add(adaptacion.original);
        }

        if (IsNaturalTipo(tipoTemp))
        {
            oDeclaracion.Desde ??= DateTime.Today;
            oDeclaracion.Hasta ??= DateTime.Today;
        }
        else
        {
            var periodoSeleccionado = GetPeriodoSeleccionado();
            if (periodoSeleccionado.HasValue)
            {
                oDeclaracion.Desde = periodoSeleccionado.Value.Desde;
                oDeclaracion.Hasta = periodoSeleccionado.Value.Hasta;
            }
        }
        Log($"seleccionFinal armada | total={seleccionFinal.Count} | toros={torosSeleccionados.Count} | certifs={certificadosSeleccionados.Count}");
        LogDetalleSnapshot("Guardar -> seleccionFinal", seleccionFinal);


        // --- USO INICIAL (lo que tenía la declaración al cargar)
        //     NOTA: usamos deseplasViejos (traídos de BD). Si estás creando, es lista vacía.
        var usoInicialPorCert = BuildUsoPorCert(deseplasViejos, multPrevio);

        // --- USO ACTUAL (lo que consumen hoy los seleccionados en pantalla)
        var usoActualPorCert = BuildUsoPorCert(seleccionFinal, multActual);

        // --- Union de HBAs para calcular delta
        var hbAs = new HashSet<string>(usoInicialPorCert.Keys);
        hbAs.UnionWith(usoActualPorCert.Keys);
        Log($"Uso certificados | inicialHBAs={usoInicialPorCert.Count} | actualHBAs={usoActualPorCert.Count} | unionHBAs={hbAs.Count}");

        // --- Calcular delta y preparar edición de certificados
        var certificadosAlterados = new List<CertifsemanDTO>();

        foreach (var hba in hbAs)
        {
            var inicial = usoInicialPorCert.TryGetValue(hba, out var i) ? i : 0;
            var actual = usoActualPorCert.TryGetValue(hba, out var a) ? a : 0;
            var delta = actual - inicial; // + = restar del certificado; - = devolver al certificado

            if (delta == 0) continue;

            var cert = certificados.FirstOrDefault(x => string.Equals(T(x.Hba), T(hba), StringComparison.OrdinalIgnoreCase));
            if (cert == null)
            {
                Log($"No se encontró certificado en memoria para HBA={Q(hba)}");
                await Swal.FireAsync("Atención", $"No se encontró certificado para HBA {hba}", SweetAlertIcon.Warning);
                continue;
            }

            // Simulamos para validar
            var nuevoNrDosi = cert.NrDosi - delta; // delta positivo resta; delta negativo suma
            if (nuevoNrDosi < 0)
            {
                Log($"Stock insuficiente | HBA={Q(hba)} | delta={delta} | actualNrDosi={cert.NrDosi}");
                await Swal.FireAsync("Atención",
                    $"El certificado HBA {hba} no tiene suficientes dosis. " +
                    $"Intento consumir {delta} y quedan {cert.NrDosi}.",
                    SweetAlertIcon.Warning);
                return; // aborta el guardado
            }

            // Creamos un "snapshot" para enviar al Editar
            certificadosAlterados.Add(new CertifsemanDTO
                {
                    Id = cert.Id,
                    NrDosi = nuevoNrDosi,

                    // <<< IMPORTANTE >>>
                    // Copiá aquí cualquier otro campo NO NULLABLE que tu API espere en Editar
                    // (si tu Editar hace "update full", envía los mismos valores actuales del cert)
                    Hba = cert.Hba,
                    Tatpart = cert.Tatpart,
                    Nrocri = cert.Nrocri,
                    NomDad = cert.NomDad
                    // ... lo que corresponda en tu DTO
                });
        }

        // --- Persistencia de certificados primero (para asegurar stock coherente)
        foreach (var cert in certificadosAlterados)
        {
            Log($"UpdateNrDosi | certId={cert.Id} | HBA={Q(cert.Hba)} | nuevoNrDosi={cert.NrDosi}");
            var r = await _certifsemanServicio.UpdateNrDosi(cert.Id, cert.NrDosi ?? 0);
            if (r?.Exito != 1)
            {
                Log($"UpdateNrDosi ERROR | certId={cert.Id} | mensaje={Q(r?.Mensaje)}");
                await Swal.FireAsync("Error", r?.Mensaje ?? "No se pudo actualizar NrDosi", SweetAlertIcon.Error);
                return; // abortar guardar si querés consistencia estricta
            }
        }

        // --- Persistir declaración y detalles (tu flujo original)
        if (!string.IsNullOrEmpty(oDeclaracion.Nrodec) && oDeclaracion.Nrodec.Length > 15)
        {
            oDeclaracion.Nrodec = oDeclaracion.Nrodec.Substring(0, 15);
        }
        else if (string.IsNullOrEmpty(oDeclaracion.Nrodec))
        {
            oDeclaracion.Nrodec = "0";
        }

        if (Id == 0)
        {
            Log("Flujo guardar: CREAR cabecera");
            // Crear: primero persistir la cabecera para reducir riesgo de desincronía en Nrodec
            Respuesta<Desepla1DTO>? createdResp = null;
            try
            {
                createdResp = await _desepla1Servicio.Crear(oDeclaracion);
            }
            catch (Exception ex)
            {
                Log($"Excepción creando cabecera: {ex.Message}");
                await Swal.FireAsync(new SweetAlertOptions
                    {
                        Title = "Error al crear declaración",
                        Html = $"No se pudo crear la cabecera.<br/><b>Detalle:</b> {System.Net.WebUtility.HtmlEncode(ex.Message)}<br/>{BuildSaveHintsHtml()}",
                        Icon = SweetAlertIcon.Error
                    });
                return;
            }

            if (createdResp == null || createdResp.Exito != 1 || createdResp.List == null)
            {
                var detalle = createdResp?.Mensaje;
                Log($"Error creando cabecera | respuestaNull={createdResp is null} | exito={createdResp?.Exito} | mensaje={Q(detalle)}");
                await Swal.FireAsync(new SweetAlertOptions
                    {
                        Title = "Error al crear declaración",
                        Html = $"No se pudo crear la cabecera.<br/><b>Detalle:</b> {System.Net.WebUtility.HtmlEncode(!string.IsNullOrWhiteSpace(detalle) ? detalle : "Sin detalle devuelto por el servidor.")}<br/>{BuildSaveHintsHtml()}",
                        Icon = SweetAlertIcon.Error
                    });
                return;
            }

            // Asegurar que usamos el Nrodec devuelto por el servidor si lo modificó
            oDeclaracion = createdResp.List;
            Log($"Cabecera creada OK | Id={oDeclaracion.Id} | Nrodec={Q(oDeclaracion.Nrodec)}");

            // Persistir detalles con el Nrodec confirmado
            foreach (var dese in seleccionFinal)
            {
                dese.Nrodec = oDeclaracion.Nrodec;
                Log($"Crear detalle | Nrodec={Q(dese.Nrodec)} | Tipo={Q(dese.Tipo)} | HBA={Q(dese.Hardb)} | Tat={Q(dese.Tatpart)} | Cantv={dese.Cantv} | Desde={Q(dese.Desde)} | Hasta={Q(dese.Hasta)}");
                try
                {
                    var detResp = await _desepla3Servicio.Crear(dese);
                    if (detResp == null || detResp.Exito != 1)
                    {
                        Log($"Error creando detalle | HBA={Q(dese.Hardb)} | tipo={Q(dese.Tipo)} | mensaje={Q(detResp?.Mensaje)}");
                        await Swal.FireAsync("Error al crear detalle",
                            $"Falló la creación de un detalle (HBA: {dese.Hardb}, Tipo: {dese.Tipo}).\nDetalle: {detResp?.Mensaje ?? "Sin detalle del servidor."}",
                            SweetAlertIcon.Error);
                        return;
                    }
                }
                catch (Exception ex)
                {
                    Log($"Excepción creando detalle | HBA={Q(dese.Hardb)} | tipo={Q(dese.Tipo)} | ex={ex.Message}");
                    await Swal.FireAsync("Error al crear detalle",
                        $"Falló la creación de un detalle (HBA: {dese.Hardb}, Tipo: {dese.Tipo}).\nDetalle: {ex.Message}",
                        SweetAlertIcon.Error);
                    return;
                }
            }
            Log($"Detalles creados: {seleccionFinal.Count}");

            Navigation.NavigateTo("/declaraciones");
            return;
        }
        else
        {
            Log("Flujo guardar: EDITAR");
            // Editar: borro viejos y recreo (como ya tenías)
            var nrodecLower = oDeclaracion.Nrodec.ToLower();
            var filtro = $"(Nrodec == null ? \"\" : Nrodec).ToLower() == \"{nrodecLower}\"";
            var existentes = await _desepla3Servicio.LimitadosFiltrados(0, 0, filtro);
            Log($"Detalles existentes a reemplazar: {existentes?.List?.Count ?? 0}");
            LogDetalleSnapshot("Editar -> detalles existentes antes de borrar", existentes?.List);

            if (existentes?.List != null)
            {
                foreach (var d in existentes.List)
                {
                    try
                    {
                        var okDelete = await _desepla3Servicio.Eliminar(d.Id);
                        if (!okDelete)
                        {
                            Log($"Error eliminando detalle previo | IdDetalle={d.Id}");
                            await Swal.FireAsync("Error al actualizar declaración",
                                $"No se pudo eliminar un detalle previo (Id {d.Id}).",
                                SweetAlertIcon.Error);
                            return;
                        }
                    }
                    catch (Exception ex)
                    {
                        Log($"Excepción eliminando detalle previo | IdDetalle={d.Id} | ex={ex.Message}");
                        await Swal.FireAsync("Error al actualizar declaración",
                            $"Falló la eliminación de un detalle previo (Id {d.Id}).\nDetalle: {ex.Message}",
                            SweetAlertIcon.Error);
                        return;
                    }
                }
            }

            foreach (var dese in seleccionFinal)
            {
                dese.Nrodec = oDeclaracion.Nrodec;
                Log($"Recrear detalle | Nrodec={Q(dese.Nrodec)} | Tipo={Q(dese.Tipo)} | HBA={Q(dese.Hardb)} | Tat={Q(dese.Tatpart)} | Cantv={dese.Cantv} | Desde={Q(dese.Desde)} | Hasta={Q(dese.Hasta)}");
                try
                {
                    var detResp = await _desepla3Servicio.Crear(dese);
                    if (detResp == null || detResp.Exito != 1)
                    {
                        Log($"Error recreando detalle | HBA={Q(dese.Hardb)} | tipo={Q(dese.Tipo)} | mensaje={Q(detResp?.Mensaje)}");
                        await Swal.FireAsync("Error al actualizar declaración",
                            $"No se pudo guardar un detalle (HBA: {dese.Hardb}, Tipo: {dese.Tipo}).\nDetalle: {detResp?.Mensaje ?? "Sin detalle del servidor."}",
                            SweetAlertIcon.Error);
                        return;
                    }
                }
                catch (Exception ex)
                {
                    Log($"Excepción recreando detalle | HBA={Q(dese.Hardb)} | tipo={Q(dese.Tipo)} | ex={ex.Message}");
                    await Swal.FireAsync("Error al actualizar declaración",
                        $"Falló la creación de un detalle (HBA: {dese.Hardb}, Tipo: {dese.Tipo}).\nDetalle: {ex.Message}",
                        SweetAlertIcon.Error);
                    return;
                }
            }

            bool okEdit;
            try
            {
                okEdit = await _desepla1Servicio.Editar(oDeclaracion);
            }
            catch (Exception ex)
            {
                Log($"Excepción editando cabecera | Id={oDeclaracion.Id} | ex={ex.Message}");
                await Swal.FireAsync("Error al actualizar declaración",
                    $"Falló la actualización de la cabecera (Id {oDeclaracion.Id}).\nDetalle: {ex.Message}",
                    SweetAlertIcon.Error);
                return;
            }

            if (!okEdit)
            {
                Log($"Error editando cabecera | Id={oDeclaracion.Id}");
                await Swal.FireAsync("Error al actualizar declaración",
                    $"No se pudo actualizar la cabecera de la declaración (Id {oDeclaracion.Id}).",
                    SweetAlertIcon.Error);
                return;
            }
            Log($"Declaración editada OK | Id={oDeclaracion.Id} | Nrodec={Q(oDeclaracion.Nrodec)} | detallesNuevos={seleccionFinal.Count}");
            Navigation.NavigateTo("/declaraciones");
            return;
        }
    }




    async Task ComeBack()
    {
        nav.NavigateTo("/declaraciones");
    }

    public class ChangeNotificationModel
    {
        public string Id { get; set; }
        public string Tipo { get; set; }
        public string Clase { get; set; }
        public string Detalle { get; set; }
    }


    async Task cancel()
    {
        Navigation.NavigateTo("/declaraciones");


    }
    private async Task<IEnumerable<SocioDTO>> BusquedaSocios(string searchText)
    {
        return await Task.FromResult(socios.Where(x => x.Criador == "S" && x.Nombre.ToLower().Contains(searchText.ToLower())).ToList());
    }
    private async Task<IEnumerable<PlantelDTO>> BusquedaPlanteles(string searchText)
    {
        var socioScod = socioSeleccionado?.Scod;
        var result = OrderPlantelesByYear(Planteles)
            .Where(x => T(x.Nrocri).Equals(T(socioScod), StringComparison.OrdinalIgnoreCase)
                && (x.Placod ?? string.Empty).ToLower().Contains((searchText ?? string.Empty).ToLower()))
            .ToList();
        Log($"BusquedaPlanteles | search={Q(searchText)} | socioScod={Q(socioScod)} | resultados={result.Count}");
        return await Task.FromResult(result);
    }

    private async Task<IEnumerable<TorosuniDTO>> BusquedaToros(string searchText)
    {
        var term = (searchText ?? string.Empty).Trim().ToLower();
        var socioScod = T(socioSeleccionado?.Scod);

        var baseQuery = toros
            .Where(x => T(x.Criador).Equals(socioScod, StringComparison.OrdinalIgnoreCase))
            .Where(x => x.CodEstado == "1")
            .Where(x => !IsToroSelected(x));

        if (!string.IsNullOrWhiteSpace(term))
        {
            baseQuery = baseQuery.Where(x =>
                (x.NomDad ?? string.Empty).ToLower().Contains(term)
                || (x.Hba ?? string.Empty).ToLower().Contains(term)
                || (x.Tatpart ?? string.Empty).ToLower().Contains(term));
        }

        return await Task.FromResult(baseQuery.Take(30).ToList());
    }

    private async Task<IEnumerable<CertifsemanDTO>> BusquedaCertificados(string searchText)
    {
        var term = (searchText ?? string.Empty).Trim().ToLower();

        var baseQuery = certificados
            .Where(x => (x.NrDosi ?? 0) > 0)
            .Where(x => !IsCertifSelected(x));

        if (!string.IsNullOrWhiteSpace(term))
        {
            baseQuery = baseQuery.Where(x =>
                (x.NomDad ?? string.Empty).ToLower().Contains(term)
                || (x.Hba ?? string.Empty).ToLower().Contains(term)
                || (x.Tatpart ?? string.Empty).ToLower().Contains(term));
        }

        return await Task.FromResult(baseQuery.Take(30).ToList());
    }

    private async Task<List<CertifsemanDTO>> LoadCertificadosForSocioAsync(int? socioId, string? socioCode)
    {
        var merged = new List<CertifsemanDTO>();

        if (socioId.HasValue && socioId.Value > 0)
        {
            var byId = await _certifsemanServicio.GetBySocioId(socioId.Value);
            var itemsById = byId.List?.ToList() ?? new List<CertifsemanDTO>();
            Log($"LoadCertificadosForSocioAsync | Socio.Id={socioId.Value} | resultadoPorId={itemsById.Count}");
            merged.AddRange(itemsById);
        }

        var codigo = T(socioCode);
        if (string.IsNullOrWhiteSpace(codigo))
        {
            return MergeCertificadosByBusinessKey(merged);
        }

        var exactFilter = BuildCertificadoFilter(codigo);
        var exact = await _certifsemanServicio.LimitadosFiltradosNoInclude(0, 0, exactFilter);
        var exactItems = exact.List?.ToList() ?? new List<CertifsemanDTO>();
        Log($"LoadCertificadosForSocioAsync | fallback exact Nrocri={Q(codigo)} | resultadoExacto={exactItems.Count}");
        merged.AddRange(exactItems);
        if (merged.Count == 0)
        {
            var containsFilter = BuildCertificadoFilter(codigo, useContains: true);
            var contains = await _certifsemanServicio.LimitadosFiltradosNoInclude(0, 0, containsFilter);
            var containsItems = (contains.List?.ToList() ?? new List<CertifsemanDTO>())
                .Where(x => x.FchUsu.HasValue)
                .ToList();
            Log($"LoadCertificadosForSocioAsync | fallback contains Nrocri={Q(codigo)} | resultadoContains={containsItems.Count}");
            merged.AddRange(containsItems);
        }

        var mergedItems = MergeCertificadosByBusinessKey(merged);
        mergedItems = OrderCertificadosByCreation(mergedItems);
        Log($"LoadCertificadosForSocioAsync | mergedTotal={mergedItems.Count}");
        LogSelectedCertificadosSnapshot("LoadCertificadosForSocioAsync preview", mergedItems);
        return mergedItems;
    }

    private void LogSelectedCertificadosSnapshot(string title, IEnumerable<CertifsemanDTO>? items)
    {
        var list = items?.ToList() ?? new List<CertifsemanDTO>();
        var resumen = string.Join(" | ", list.Take(10).Select(c =>
            $"Id={c.Id},HBA={Q(c.Hba)},Nom={Q(c.NomDad)},Nrocri={Q(c.Nrocri)},NrDosi={c.NrDosi},FchUsu={(c.FchUsu.HasValue ? c.FchUsu.Value.ToString("yyyy-MM-dd") : "<null>")}"));
        Log($"{title} | total={list.Count} | {resumen}");
    }

    private static List<CertifsemanDTO> MergeCertificadosByBusinessKey(IEnumerable<CertifsemanDTO> items)
    {
        var unique = new List<CertifsemanDTO>();
        var seen = new HashSet<string>(StringComparer.OrdinalIgnoreCase);

        foreach (var item in items.Where(x => x != null))
        {
            var key = BuildCertificadoUniqueKey(item);
            if (seen.Add(key))
            {
                unique.Add(item);
            }
        }

        return unique;
    }

    private static List<CertifsemanDTO> OrderCertificadosByCreation(IEnumerable<CertifsemanDTO>? items)
        => (items ?? Enumerable.Empty<CertifsemanDTO>())
            .OrderByDescending(x => x.FchUsu.HasValue)
            .ThenByDescending(x => x.FchUsu)
            .ThenByDescending(x => x.Id)
            .ToList();

    private static string BuildCertificadoUniqueKey(CertifsemanDTO item)
    {
        if (item.Id > 0)
        {
            return $"ID:{item.Id}";
        }

        return $"BUS:{NormalizeKeyPart(item.NroCert)}|{NormalizeKeyPart(item.Hba)}|{NormalizeKeyPart(item.Tatpart)}";
    }

    private static string NormalizeKeyPart(string? value) => (value ?? string.Empty).Trim();

    private static string BuildCertificadoFilter(string? socioScod, bool useContains = false)
    {
        var value = EscapeForFilter((socioScod ?? string.Empty).Trim());
        var nrocriFilter = useContains
            ? $"(Nrocri == null ? \"\" : Nrocri).ToLower().Contains(\"{value.ToLower()}\")"
            : $"Nrocri == \"{value}\"";

        return $"({nrocriFilter}) && (FchUsu != null)";
    }

    private static string BuildCertificadoHbaFilter(string hba)
    {
        var value = EscapeForFilter((hba ?? string.Empty).Trim().ToLower());
        return $"(Hba == null ? \"\" : Hba).ToLower() == \"{value}\" && (FchUsu != null)";
    }

    private static List<PlantelDTO> OrderPlantelesByYear(IEnumerable<PlantelDTO>? items)
        => (items ?? Enumerable.Empty<PlantelDTO>())
            .OrderByDescending(x => TryGetPlantelYear(x).HasValue)
            .ThenByDescending(x => TryGetPlantelYear(x))
            .ThenByDescending(x => x.Id)
            .ToList();

    private static string FormatoPlantel(PlantelDTO? plantel)
    {
        if (plantel is null)
        {
            return string.Empty;
        }

        var placod = plantel.Placod?.Trim() ?? string.Empty;
        var anio = plantel.Anioex?.Trim() ?? string.Empty;

        if (string.IsNullOrWhiteSpace(anio))
        {
            return placod;
        }

        if (string.IsNullOrWhiteSpace(placod))
        {
            return anio;
        }

        return $"{placod} - {anio}";
    }

    private static int? TryGetPlantelYear(PlantelDTO? plantel)
    {
        if (plantel == null || string.IsNullOrWhiteSpace(plantel.Anioex))
        {
            return null;
        }

        return int.TryParse(plantel.Anioex, out var year) ? year : null;
    }

    private static bool IsDetalleInseminacion(string? tipo)
        => DeclaracionServicioHelper.IsDetalleInseminacion(tipo);

    private PlantelDTO? ResolvePlantelForDeclaracion(string? nroplan, DateTime? fechaDeclaracion, out string warning)
        => DeclaracionServicioHelper.ResolvePlantelHistorico(Planteles, nroplan, fechaDeclaracion, out warning);

    private async Task OnToroSeleccionado(TorosuniDTO? toro)
    {
        if (toro == null) return;
        await ToggleToro(toro);
        toroSeleccionado = null;
        await InvokeAsync(StateHasChanged);
    }

    private async Task OnCertifSeleccionado(CertifsemanDTO? certif)
    {
        if (certif == null) return;
        await ToggleCertif(certif);
        certifSeleccionado = null;
        await InvokeAsync(StateHasChanged);
    }

    private async Task OnPlantelSeleccionado(PlantelDTO? plantel)
    {
        Plantelseleccionado = plantel;
        plantelLoadWarning = string.Empty;
        await InvokeAsync(StateHasChanged);
    }

    protected async Task AddToSelectedToros(DataGridRowMouseEventArgs<TorosuniDTO> args)
        => await ToggleToro(args?.Data);

    private bool IsToroSelected(TorosuniDTO? toro)
    {
        var key = BuildToroSelectionKey(toro);
        return !string.IsNullOrWhiteSpace(key)
            && torosSeleccionados.Any(t => string.Equals(BuildToroSelectionKey(t), key, StringComparison.OrdinalIgnoreCase));
    }

    private bool IsCertifSelected(CertifsemanDTO? certif)
        => certif != null && certificadosSeleccionados.Any(c => string.Equals(T(c.original?.Hardb), T(certif.Hba), StringComparison.OrdinalIgnoreCase));

    private async Task ToggleToro(TorosuniDTO? toro)
    {
        if (toro == null)
        {
            Log("ToggleToro ignorado (toro null)");
            return;
        }

        var selectionKey = BuildToroSelectionKey(toro);
        if (string.IsNullOrWhiteSpace(selectionKey))
        {
            Log($"ToggleToro ignorado (sin clave de selección) | Id={toro.Id} | NomDad={Q(toro.NomDad)} | HBA={Q(toro.Hba)} | Tat={Q(toro.Tatpart)}");
            return;
        }

        var existente = torosSeleccionados.FirstOrDefault(t => string.Equals(BuildToroSelectionKey(t), selectionKey, StringComparison.OrdinalIgnoreCase));
        if (existente != null)
        {
            Log($"ToggleToro ignorado (ya seleccionado) | key={selectionKey} | HBA={Q(toro.Hba)}");
            return;
        }

        torosSeleccionados.Add(
            new Desepla3Data
            {
                LineKey = Guid.NewGuid(),
                original = new Desepla3DTO
                {
                    Hardb = toro.Hba,
                    Nrodec = nrodec,
                    Tatpart = toro.Tatpart,
                    Tipo = "toro"
                },
                TipToro = toro.TipToro,
                SelectionKey = selectionKey,
                Id = toro.Id,
                Desde = DateTime.Today,
                Hasta = DateTime.Today
            }
        );
        Log($"ToggleToro ADD | key={selectionKey} | Id={toro.Id} | HBA={Q(toro.Hba)} | TipToro={Q(toro.TipToro)} | totalTorosSel={torosSeleccionados.Count}");

        try { if (gridToro1 != null) await gridToro1.Reload(); } catch { }
        try { if (gridToro2 != null) await gridToro2.Reload(); } catch { }
        await InvokeAsync(StateHasChanged);
    }



    private async Task RemoveToro(Desepla3Data data)
    {
        var before = torosSeleccionados.Count;
        torosSeleccionados = torosSeleccionados
            .Where(t => t.LineKey != data.LineKey)
            .ToList();
        Log($"RemoveToro | before={before} | after={torosSeleccionados.Count} | HBA={Q(data?.original?.Hardb)}");

        try { if (gridToro1 != null) await gridToro1.Reload(); } catch { }
        try { if (gridToro2 != null) await gridToro2.Reload(); } catch { }
        await InvokeAsync(StateHasChanged);
    }



    private async Task RemoveCertif(Desepla3Data data)
    {
        var before = certificadosSeleccionados.Count;
        certificadosSeleccionados = certificadosSeleccionados
            .Where(c => c.LineKey != data.LineKey)
            .ToList();
        Log($"RemoveCertif | before={before} | after={certificadosSeleccionados.Count} | HBA={Q(data?.original?.Hardb)}");

        try { if (gridCertif != null) await gridCertif.Reload(); } catch { }
        await InvokeAsync(StateHasChanged);
    }






    protected async Task AddToSelectedCertif(DataGridRowMouseEventArgs<CertifsemanDTO> args)
        => await ToggleCertif(args?.Data);

    private async Task ToggleCertif(CertifsemanDTO? certif)
    {
        if (certif == null || string.IsNullOrWhiteSpace(certif.Hba))
        {
            Log("ToggleCertif ignorado (certif null o HBA vacío)");
            return;
        }

        var existente = certificadosSeleccionados.FirstOrDefault(c => string.Equals(T(c.original?.Hardb), T(certif.Hba), StringComparison.OrdinalIgnoreCase));
        if (existente != null)
        {
            Log($"ToggleCertif ignorado (ya seleccionado) | HBA={Q(certif.Hba)}");
            return;
        }

        certificadosSeleccionados.Add(
            new Desepla3Data
            {
                LineKey = Guid.NewGuid(),
                original = new Desepla3DTO
                {
                    Hardb = certif.Hba,
                    Nrodec = nrodec,
                    Tatpart = certif.Tatpart,
                    Tipo = "insem",
                    Cantv = 0
                },
                Desde = DateTime.Today,
                Hasta = DateTime.Today
            }
        );
        Log($"ToggleCertif ADD | HBA={Q(certif.Hba)} | Nrocri={Q(certif.Nrocri)} | totalCertSel={certificadosSeleccionados.Count}");

        try { if (gridCertif != null) await gridCertif.Reload(); } catch { }
        await InvokeAsync(StateHasChanged);
    }

    protected async Task CleanSelected()
    {
        Log($"CleanSelected | torosAntes={torosSeleccionados.Count} | certsAntes={certificadosSeleccionados.Count}");
        torosSeleccionados = new List<Desepla3Data>();
        certificadosSeleccionados = new List<Desepla3Data>();

        try { if (gridToro1 != null) await gridToro1.Reload(); } catch { }
        try { if (gridToro2 != null) await gridToro2.Reload(); } catch { }
        try { if (gridCertif != null) await gridCertif.Reload(); } catch { }

        StateHasChanged();
        Log("CleanSelected END");
    }


    protected async Task UpdateValores(SocioDTO socio)
        => await LoadSocioDataAsync(socio, preserveCabecera: _isEdit, refreshUi: true);

    private async Task LoadSocioDataAsync(SocioDTO socio, bool preserveCabecera, bool refreshUi)
    {
        socioSeleccionado = socio;
        isLoadingDependencias = true;
        loadingMessage = "Cargando planteles, toros y certificados...";
        if (refreshUi)
        {
            await InvokeAsync(StateHasChanged);
        }

        var socioScodRaw = socio?.Scod ?? string.Empty;
        var socioScodTrim = T(socio?.Scod);
        Log($"UpdateValores START | socio.Id={socio?.Id} | socio.ScodRaw={Q(socioScodRaw)} | socio.ScodTrim={Q(socioScodTrim)}");

        await ResetForSocioChange(preserveCabecera: preserveCabecera, refreshUi: false, reloadGrids: false);

        var filtro = $"Nrocri == \"{EscapeForFilter(socioScodRaw)}\"";
        Log($"UpdateValores -> Planteles filtro: {filtro}");
        var plantelesTask = _plantelServicio.LimitadosFiltradosNoInclude(0, 0, filtro);

        filtro = $"Criador == \"{EscapeForFilter(socioScodRaw)}\"";
        Log($"UpdateValores -> Toros filtro: {filtro}");
        var torosTask = _torosServicio.LimitadosFiltradosNoInclude(0, 0, filtro);

        Log($"UpdateValores -> Certificados filtro por Socio.Id: {socio?.Id}");
        var certificadosTask = LoadCertificadosForSocioAsync(socio?.Id, socioScodRaw);

        await Task.WhenAll(plantelesTask, torosTask, certificadosTask);

        var rt = await plantelesTask;
        Planteles = OrderPlantelesByYear(rt.List);
        Log($"Planteles cargados: {Planteles?.Count ?? 0}");
        if ((Planteles?.Count ?? 0) == 0 && !string.IsNullOrWhiteSpace(socioScodTrim))
        {
            var filtroDiag = $"(Nrocri == null ? \"\" : Nrocri).ToLower().Contains(\"{EscapeForFilter(socioScodTrim.ToLower())}\")";
            var rtDiag = await _plantelServicio.LimitadosFiltradosNoInclude(0, 20, filtroDiag);
            Planteles = OrderPlantelesByYear(rtDiag?.List)
                .Where(x => T(x.Nrocri).Equals(socioScodTrim, StringComparison.OrdinalIgnoreCase))
                .ToList();
            Log($"Diag Planteles (contains scod trim) -> {rtDiag?.List?.Count ?? 0} resultados crudos | filtradosExactos={Planteles.Count} | filtro={filtroDiag}");
        }

        var rta = await torosTask;
        toros = rta.List ?? new List<TorosuniDTO>();
        Log($"Toros cargados: {toros?.Count ?? 0}");
        if ((toros?.Count ?? 0) == 0 && !string.IsNullOrWhiteSpace(socioScodTrim))
        {
            var filtroDiag = $"(Criador == null ? \"\" : Criador).ToLower().Contains(\"{EscapeForFilter(socioScodTrim.ToLower())}\")";
            var rtDiag = await _torosServicio.LimitadosFiltradosNoInclude(0, 20, filtroDiag);
            toros = (rtDiag?.List ?? new List<TorosuniDTO>())
                .Where(x => T(x.Criador).Equals(socioScodTrim, StringComparison.OrdinalIgnoreCase))
                .ToList();
            Log($"Diag Toros (contains scod trim) -> {rtDiag?.List?.Count ?? 0} resultados crudos | filtradosExactos={toros.Count} | filtro={filtroDiag}");
        }
        if ((toros?.Count ?? 0) > 0)
        {
            var criadores = toros.Select(t => t.Criador).Where(c => !string.IsNullOrWhiteSpace(c)).Distinct().Take(5);
            Log($"Toros criadores (muestra): {string.Join(" | ", criadores.Select(Q))}");
        }

        certificados = await certificadosTask;
        Log($"Certificados cargados: {certificados?.Count ?? 0}");
        if ((certificados?.Count ?? 0) > 0)
        {
            var nrocris = certificados.Select(c => c.Nrocri).Where(n => !string.IsNullOrWhiteSpace(n)).Distinct().Take(5);
            Log($"Certificados Nrocri (muestra): {string.Join(" | ", nrocris.Select(Q))}");
        }

        isLoadingDependencias = false;
        if (refreshUi)
        {
            await InvokeAsync(StateHasChanged);
        }
        Log($"UpdateValores END | Planteles={Planteles?.Count ?? 0} | Toros={toros?.Count ?? 0} | Certifs={certificados?.Count ?? 0}");
    }




    private async Task ResetForSocioChange(bool preserveCabecera = false, bool refreshUi = true, bool reloadGrids = true)
    {
        Log($"ResetForSocioChange START | preserveCabecera={preserveCabecera}");
        torosSeleccionados.Clear();
        certificadosSeleccionados.Clear();
        seleccionFinal.Clear();
        deseplasViejos.Clear();
        plantelLoadWarning = null;

        Plantelseleccionado = null;

        if (!preserveCabecera)
        {
            tipoTemp = string.Empty;
            oDeclaracion.Cantv = 0;
            oDeclaracion.Cantb = 0;
            oDeclaracion.Desde = DateTime.Today;
            oDeclaracion.Hasta = DateTime.Today;
        }

        if (reloadGrids)
        {
            try { if (gridToro1 != null) await gridToro1.Reload(); } catch { }
            try { if (gridToro2 != null) await gridToro2.Reload(); } catch { }
            try { if (gridCertif != null) await gridCertif.Reload(); } catch { }
        }

        if (refreshUi)
        {
            await InvokeAsync(StateHasChanged);
        }
        Log("ResetForSocioChange END");
    }








    public class Desepla3Data
    {
        public Guid LineKey { get; set; } = Guid.NewGuid();
        public Desepla3DTO original { get; set; }
        public DateTime Desde { get; set; } = DateTime.Today;
        public DateTime Hasta { get; set; } = DateTime.Today;
        public string? TipToro { get; set; } = "";
        public string SelectionKey { get; set; } = "";
        public int Id { get; set; }
    }


    double GetMultiplicadorForCode(string tipse) => DeclaracionServicioHelper.GetMultiplicadorForCode(tipse);

    

 
    async Task EliminarDeclaracion()
    {
        Log($"EliminarDeclaracion START | Id={oDeclaracion?.Id} | Nrodec={Q(oDeclaracion?.Nrodec)} | Tipse={Q(oDeclaracion?.Tipse)}");
        var result = await Swal.FireAsync(new SweetAlertOptions
            {
                Title = "¿Eliminar declaración?",
                Text = "Se eliminará la declaración y se restaurarán las dosis.",
                Icon = SweetAlertIcon.Warning,
                ShowCancelButton = true,
                ConfirmButtonText = "Sí, eliminar",
                CancelButtonText = "Cancelar"
            });

        if (!result.IsConfirmed) return;

        if (oDeclaracion is null || string.IsNullOrWhiteSpace(oDeclaracion.Nrodec))
        {
            await Swal.FireAsync("Atención", "No se encontró la declaración a eliminar.", SweetAlertIcon.Warning);
            return;
        }

        try
        {
            // 1) Traer TODOS los detalles (Desepla3) por Nrodec
            var nrodecLower = oDeclaracion.Nrodec.ToLower();
            var filtro = $"(Nrodec == null ? \"\" : Nrodec).ToLower() == \"{nrodecLower}\"";
            var detallesResp = await _desepla3Servicio.LimitadosFiltrados(0, 0, filtro);
            var detalles = detallesResp?.List?.ToList() ?? new List<Desepla3DTO>();
            Log($"EliminarDeclaracion | detalles encontrados={detalles.Count} | filtro={filtro}");


            // 2) Calcular cuántas dosis devolver por HBA (solo líneas de inseminación)
            var mult = GetMultiplicadorForCode(oDeclaracion.Tipse ?? "");
            var devolverPorHba = detalles
                .Where(d => (d.Tipo ?? "").Equals("insem", StringComparison.OrdinalIgnoreCase))
                .Select(d => new { Detail = d, Hba = (d.Hardb ?? string.Empty).Trim() })
                .Where(x => !string.IsNullOrWhiteSpace(x.Hba))
                .GroupBy(x => x.Hba)
                .ToDictionary(
                    g => g.Key,
                    g => g.Sum(x => DosisNecesarias(x.Detail.Cantv, mult))
                );
            Log($"EliminarDeclaracion | HBAs a devolver={devolverPorHba.Count}");

            // 3) Asegurar certificados en memoria (si está vacío, recargar)
            if (certificados == null || certificados.Count == 0)
            {
                certificados = await LoadCertificadosForSocioAsync(socioSeleccionado?.Id, oDeclaracion.Nrocri);
            }

            // 4) Restaurar NrDosi por cada HBA involucrado
            foreach (var kvp in devolverPorHba)
            {
                var hba = kvp.Key;
                var cantidadADevolver = kvp.Value;

                if (string.IsNullOrWhiteSpace(hba) || cantidadADevolver <= 0) continue;

                var cert = certificados.FirstOrDefault(c => string.Equals(T(c.Hba), hba, StringComparison.OrdinalIgnoreCase));
                if (cert == null)
                {
                    var f = BuildCertificadoHbaFilter(hba);
                    var uno = await _certifsemanServicio.LimitadosFiltradosNoInclude(0, 1, f);
                    cert = uno.List?.FirstOrDefault();
                    if (cert != null) certificados.Add(cert);
                }

                if (cert == null)
                {
                    await Swal.FireAsync("Atención", $"No se encontró certificado para HBA {hba} al restaurar dosis.", SweetAlertIcon.Warning);
                    continue;
                }

                var nuevo = (cert.NrDosi ?? 0) + cantidadADevolver;

                var r = await _certifsemanServicio.UpdateNrDosi(cert.Id, nuevo);
                if (r?.Exito != 1)
                {
                    await Swal.FireAsync("Error", r?.Mensaje ?? $"Error al restaurar dosis para HBA {hba}.", SweetAlertIcon.Error);
                    return;
                }

                cert.NrDosi = nuevo;
                Log($"EliminarDeclaracion | dosis restaurada | certId={cert.Id} | HBA={Q(hba)} | nuevoNrDosi={nuevo}");
            }

            foreach (var d in detalles)
            {
                try { await _desepla3Servicio.Eliminar(d.Id); }
                catch { /* loggear si querés */ }
            }

            await _desepla1Servicio.Eliminar(oDeclaracion.Id);
            Log("EliminarDeclaracion END OK");

            Navigation.NavigateTo("/declaraciones");
        }
        catch (Exception ex)
        {
            Log($"EliminarDeclaracion ERROR: {ex}");
            await Swal.FireAsync("Error", "Ocurrió un error al eliminar la declaración.", SweetAlertIcon.Error);
        }
    }




    private async Task OnTipoChanged(object value)
    {
        var nuevo = value?.ToString() ?? "";
        Log($"OnTipoChanged | anterior={Q(tipoTemp)} | nuevo={Q(nuevo)}");

        var tipoAnterior = tipoTemp;
        previousTipoTemp = tipoTemp;
        var nuevoNormalizado = NormalizeTipoLabel(nuevo);

        if (_isEdit && !string.Equals(nuevoNormalizado, tipoAnterior, StringComparison.Ordinal))
        {
            var haySeleccion = torosSeleccionados.Count > 0 || certificadosSeleccionados.Count > 0;
            var hayDetallesPrevios = deseplasViejos.Count > 0;

            if (haySeleccion || hayDetallesPrevios)
            {
                var result = await Swal.FireAsync(new SweetAlertOptions
                    {
                        Title = "Cambiar tipo de servicio",
                        Html = "Al cambiar el tipo se quitará todo lo seleccionado en pantalla.<br/>" +
                               "Nada se guarda en la base hasta que presiones Guardar. Las dosis se devolverán recién cuando guardes la declaración.",
                        Icon = SweetAlertIcon.Warning,
                        ShowCancelButton = true,
                        ConfirmButtonText = "Sí, cambiar",
                        CancelButtonText = "Cancelar"
                    });

                if (!result.IsConfirmed)
                {
                    tipoTemp = tipoAnterior;
                    await InvokeAsync(StateHasChanged);
                    Log("OnTipoChanged cancelado por usuario.");
                    return;
                }
            }
        }

        tipoTemp = nuevoNormalizado;
        await CleanSelected();
    }

}
