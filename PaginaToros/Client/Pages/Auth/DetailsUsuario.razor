@attribute [Authorize(Roles = "USUARIOMAESTRO")]

<AuthorizeView Roles="USUARIOMAESTRO,ADMINISTRADOR">
    <div class="row" style="overflow-y: auto; max-height: 650px;">
        <UserInfo Usuario="Usuario" />
    </div>
    <hr />
    @*<button @onclick="ModalInstance.CancelAsync" class="btn btn-secondary">Cerrar</button>*@
</AuthorizeView>

@code {
    [CascadingParameter] BlazoredModalInstance ModalInstance { get; set; }
    [Parameter] public Usuario Usuario { get; set; }
}
